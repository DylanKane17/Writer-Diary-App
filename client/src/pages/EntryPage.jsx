export default function EntryPage(props) {
  //pass in the id and access data of the entry via the id

  //need to return each one as an object with an edit function

  return (
    <main>
      <Link to="/">Back</Link>
    </main>
  );
}
